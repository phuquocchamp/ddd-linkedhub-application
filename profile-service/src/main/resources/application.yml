spring:
  application:
    name: profile-service
  profiles:
    active: dev
  config:
    import: optional:configserver:http://localhost:8888/

  cloud:
    stream:
      default-binder: rabbit

springdoc:
  swagger-ui:
    enabled: false
  api-docs:
    enabled: true
    path: /v3/api-docs